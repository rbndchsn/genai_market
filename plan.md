# GenAI_Market — Project Plan

This file is the living source of truth for this project. Every agent reads it first
and updates it as work lands. See `CLAUDE.md` for the rules.

---

## Current Status

| Field | Value |
|---|---|
| Last updated | 2026-09-17 |
| Current phase | Phase 7 (QA and launch) in progress. Site live at https://rbndchsn.github.io/genai_market/ with nine pages. The full accuracy evaluation (7.2b) is done and published on the "How it was built" page. 7.3 (Lighthouse) and 7.5 (README) done and pushed on 2026-09-17 (4ac3f27). 7.6 (licensing for reuse) done and pushed (f6ba215); both deploys succeeded. Phase 6 (chat) is paused. |
| Next step | Ask the user before any further work. Remaining options: 7.2c glossary accuracy check (optional, see the step note); 7.4 custom domain (depends on Q4); resuming Phase 6, the chat (depends on Q3); Phase 8 maintenance. A live Lighthouse re-run to confirm the local 7.3 results was offered on 2026-09-17 and declined; do it only if the user asks (runner: install `lighthouse@12` in a scratch folder and drive it from Node; PowerShell splits comma lists in `--only-categories`). Standing site rules: no data downloads, no citation section, no corrections channel; the repository link and the build method are published on purpose. Browsers cache `js/` and `css/` for ten minutes after a deploy (Pages `max-age=600`); a version stamp on those links was offered and not requested. |
| Blockers | None. Q4 (site name) still open; the shell uses the working name GenAI Market, which is now public on the live site. Q3 only matters if Phase 6 resumes. |

Repository: https://github.com/rbndchsn/genai_market (public, `main`). Commit only when the user asks. `content/synth/`, `content/raw/` and `sourcedoc/` are git-ignored; the synth notes exist only on the local machine, so do not delete them.

Hosting: GitHub Pages from the repo above (Q2 resolved), published by `.github/workflows/pages.yml` from `site/` on every push to `main`. Live at https://rbndchsn.github.io/genai_market/. No Cloudflare in the MVP. The licensed AIM PDF (`sourcedoc/`) and every extraction of it (`content/raw/`) must never be committed; the repo is public.

Source inventory: 12 sources, all synthesised (`content/synth/src-01.md` to `src-12.md`). PDFs in `sourcedoc/` (9), web pages in `content/raw/src-07,08,12/page.md`. Data files: 12 sources, 164 glossary terms, 95 insights (47 multi-source, 32 single-source, 16 agent-analysis, after the 7.2 and 7.2b evaluations), 28 vendors, 259 stats, 599 chunks.

### Phase 5 build and review routine (handoff for any session)

Read this before touching `site/`. It is what a fresh session needs to continue at the same standard.

**Layout.** Static files only, no build step, all paths relative (the site lives under `/genai_market/` on GitHub Pages). Pages: `index.html`, `insights.html`, `vendors.html`, `stats.html`, `glossary.html`, `sources.html`, `search.html`, `404.html`. Page scripts: `home.js`, `insights.js`, `vendors.js`, `stats.js`, `glossary.js`, `sources.js`, `search.js`. Check scripts: `scripts/review_shots.py` (screenshots), `scripts/check_site.py` (links, anchors, console errors). Each page has the same head block (see any existing page: viewport, description, favicon, `css/site.css`, an inline script that applies a stored theme before first paint), a `<body data-page="key">` (keys: home, insights, vendors, stats, glossary, sources) and a `<main id="main" class="container">`. Every page loads `js/shell.js` first and its own `js/<page>.js` after. Placeholder bodies use `<div class="placeholder">` and are replaced step by step.

**Shell helpers (`site/js/shell.js`, exposed as `window.GM`).** `GM.loadJSON(name)` fetches and caches `data/<name>.json`; `GM.escapeHTML(s)`; `GM.formatRefs(refs, sourcesById)` renders "Publisher, year, p. N" with links to `sources.html#src-NN`; `GM.shortName(id, source)` gives the short publisher name (AIM Research, IDC and Solace, McKinsey, Deloitte, BCG, MIT NANDA, Menlo Ventures, a16z, KPMG US, Microsoft, Stanford HAI, IDC FutureScape); `GM.currentTheme()` returns light or dark. The shell renders the skip link, header, nav (active item from `data-page`), footer and theme toggle; pages never duplicate that markup.

**CSS (`site/css/site.css`).** Tokens on `:root` (colours, spacing `--space-1..8`, `--radius`, `--container` 1100px, `--gutter` 16px). Dark theme is defined twice: under `@media (prefers-color-scheme: dark)` guarded by `:root:not([data-theme="light"])`, and under `:root[data-theme="dark"]`. Chart tokens `--series-1..8`, `--seq-250..650`, `--div-neg/mid/pos`, `--viz-grid/axis/muted/deemph` exist in both themes and were validated with the `dataviz` palette checker against our surfaces (#ffffff light, #1d2025 dark). Components available: `.card`, `a.card`, `.grid`, `.stack`, `.pill` (`.pill-accent`, `.pill-warn`, `a.pill`), `.chip`, `.notice`, `.placeholder`, `.ref`, `.filters` (label + select/input row), `.count-line`, `.theme-head`, `.insight` card with `<details>`, `.stat-tile`, `.hero-figure`, `.kpi-row`, `.sources-strip`, `.table-wrap`, `.visually-hidden`. `html` has `scroll-padding-top` for the sticky header. Never nest an `<a>` inside an `<a>` (refs are links; make the container a `<div>` or `<article>`).

**Data conventions.** Every number on the site is read from `site/data/*.json` at runtime; never type a statistic into HTML. Web-source refs have `url`, PDF refs have `pages`. Insight, stat, term and vendor ids are stable anchors (`insights.html#ins-045`, `stats.html#s4-02`, `glossary.html#sovereign-ai`, `vendors.html#blend360`, `sources.html#src-04`), so every page must render `id` attributes on its records. Filters that a page offers are mirrored in the query string (see `insights.js` for the pattern) so views are shareable; hash deep links must open and scroll to the record and override filters.

**Charts (5.4, 5.5).** Load the `dataviz` skill before writing any chart or stat tile. Rules that bind here: one hero figure per view; KPI rows are stat tiles, not bar charts; categorical hues in fixed slot order, never cycled; all-pairs forms (scatter, bubble) use at most three hues; one axis, never dual; thin marks, 2px gaps between fills, direct labels for four or fewer series and a legend for two or more; hover tooltips on every mark; a table view for every chart (`.table-wrap`); text in text tokens, never series colour; three light slots (aqua, yellow, magenta) are under 3:1 so charts using them need direct labels or the table. Series with more than about 12 points (s4-35, s3-25, s3-31, s11-22) need grouped or small-multiple treatment. Validate any new palette with the checker in the skill's `scripts/validate_palette.js` against both surfaces.

**Review routine, run before marking any step done.** `python scripts/review_shots.py "<page>?<query>#<hash>" [--height N] [--theme light|dark] [--iframe] [--out DIR]` starts a local server, screenshots the page at 1280px and inside a 390px iframe (desktop Chrome cannot shrink below about 500px, so the iframe is the phone view), prints the file paths and the rendered DOM size. Open both images with the Read tool and check: nothing overflows horizontally, no label collisions, data actually rendered (DOM size in the thousands), the active nav item is right, dark and light both work (`--theme dark` and `--theme light` store the preference through a same-origin harness; without it headless Chrome follows the machine's colour scheme), deep links open their target. Pages that open a modal `<dialog>` need `--iframe`, because headless Chrome paints a top-level page blank while the top layer is open. For DOM-level checks use the browser's `--dump-dom` with `--virtual-time-budget=6000` and grep. `node --check site/js/<file>.js` for syntax. Fix every defect found before recording the step.

**Recording and committing.** When a step is done: tick it in Implementation Steps with date, files, what was verified and what was fixed; update Current Status (next step); add a Change History row. Commit only when the user asks (they have said "do it" after each step so far; ask once whether that is standing permission). Commit message "Step 5.N: <name>" plus the attribution line the harness provides. Before every commit, `git diff --cached --name-only | grep -Ei "sourcedoc|content/raw|content/synth|originality-check"` must return nothing. Push to `origin main`.

**Decisions still open for the user:** Q4 site name (GenAI Market until told otherwise). On 2026-09-16 the user asked for all of Phase 5 to be finished in one run, so 5.4 to 5.10 proceed without per-step checkpoints, each step committed and pushed as it lands (5.10 needs the pushes).

Phase 5 reading guide: `site/data/insights.json` has 12 themes and 95 insights (ins-085 to ins-095 are the outlook set); `stats.json` records carry `chart` hints (bar, grouped-bar, donut, range, table, none) and a `series_label`; every record's `refs` resolve to `sources.json`. Series with more than about 12 points (for example s4-35, s3-25, s3-31, s11-22) need a grouped or small-multiple treatment. Menlo (src-07) and a16z (src-08) vendor-share stats must be shown side by side with their conflict-of-interest notes (see ins-045).

Web-source charts: `extract_web.py` saves only text. When a web source's charts carry numbers the prose does not, fetch the chart images into the session scratchpad, convert to PNG and read them there. Do not save them into the project (no source images are kept).

How to do step 4.2: follow `content/synth/README.md` exactly (read the full extraction, verify every chart against the page render, write the notes in the fixed structure, update this plan, report). Use `src-05.md` as the model for survey-type sources.

Data pipeline (Phase 3 output): edit `site/data/{sources,glossary,insights,vendors,taxonomy,stats}.json` by hand, then run `python scripts/validate_data.py`, `python scripts/build_chunks.py` and `python scripts/build_search_index.py`. Never edit `chunks.json` or `search-index.json` directly. Schemas are in `content/schemas.md`.

Phase 3 reading guide: `content/raw/<src-id>/text.md` for prose, `content/raw/<src-id>/pages/NNN.png` for any table, chart, or the PeMa quadrant (src-01 quadrant section starts p25, vendor profiles start p30, one vendor per two pages; src-02 appendix tables p23-25).

---

## 1. Project Overview

**What we are building:** A public, interactive website that presents a **meta-analysis**
of the current and future state of AI adoption in companies, products, and services.
We read multiple sources (analyst PDFs, industry surveys, reputable web publications),
merge and reconcile what they say, and publish our own research findings with
references. The site makes those findings navigable, searchable, and visual: key
insights, vendor landscape, survey statistics, a glossary, full-text search, and a
natural-language "ask the research" chat.

**Framing:** This is original research output built on cited sources, in the same way
an academic literature review or a market meta-analysis cites its inputs. We do not
republish any source. We report what the body of evidence shows and reference where
each finding comes from.

**Who it is for:** Business and technology decision-makers, consultants, and analysts
who need to understand the GenAI services market and AI adoption trends without
reading 100+ pages of reports.

**Problem it solves:** Analyst reports are long, dense, and impossible to hold in your
head. The valuable content (rankings, stats, challenges, what leaders do differently)
is locked in PDF layouts that nobody reads end to end. The site unpacks that content
into structured, cross-referenced, interactive form.

**Working name:** GenAI Market (rename later if needed, see Q4).

---

## 2. Goals and Non-Goals

### Goals
- Read every source document in full and synthesize it into structured data (JSON) with page-level citations.
- Present the content as a fast, responsive, static website that works on phone and desktop.
- Provide: insights dashboard, vendor explorer with interactive quadrant, glossary, full-text search, sources page, and an ask-the-report chat.
- Supplement the PDFs with 5 to 10 reputable web sources on AI adoption trends, each cited with URL and date.
- Make the content pipeline repeatable so new PDFs and web sources can be added in later phases.
- Attribute every finding to one or more sources. Write everything in our own words, as a meta-analysis.
- Where sources agree, say so and cite all of them. Where they differ, present the difference.

### Non-Goals
- Not a general AI news site. Scope is enterprise AI adoption and the GenAI services market.
- No user accounts, comments, or CMS in v1.
- No verbatim text from the AIM / Blend360 report (SRC-01). Not even short quotes. Findings only, in our words, with a reference.
- No republication of any source document, chart, or image. Charts are rebuilt from data we report.
- No server-side rendering. The only backend is the chat Worker.

---

## 3. Source Material

| ID | File | Pages | Publisher / date | Description | Reuse notes |
|---|---|---|---|---|---|
| SRC-01 | `sourcedoc/Report_GenAI_Service_Providers_PeMa_Quadrant_2026 (1).pdf` | 90 | AIM Research, April 2026 | Top Generative AI Service Providers 2026, Penetration Maturity (PeMa) Quadrant. Market outlook, key challenges, service taxonomy, vendor profiles. 156 embedded images (charts, quadrant). | Licensed copy. **No verbatim text, no images, no reproduced charts.** Findings in our words with a reference to AIM Research 2026. |
| SRC-02 | `sourcedoc/state-of-real-time-data-2026-idc.pdf` | 28 | IDC Custom Solutions, September 2026, sponsored by Solace | 2026 State of Real-Time Data: Agentic Enterprises Are Running on Real-Time Data. Survey-based InfoBrief with four maturity tiers, industry and company-size breakdowns, what leaders do differently. Accessible data tables in the appendix (p23 to 25). | Free sponsored InfoBrief. Cite IDC and Solace. Appendix tables are the primary stats source. |
| SRC-03+ | Web sources | — | Added in Phase 4 | Stanford AI Index, McKinsey State of AI, Gartner, Deloitte, Menlo Ventures, etc. | Record in `data/sources.json` with URL, publisher, date, accessed date. |

Extraction tooling: the workspace Python at `C:\Projects\MyPythonProjects\ENV\Scripts\python.exe`
has PyMuPDF (`fitz`) and `docling` installed. Both PDFs have embedded TOC bookmarks,
which can be used for section splitting.

---

## 4. Architecture / Approach

**Tech stack:** Static HTML, CSS, and vanilla JavaScript. Content lives in JSON files
under `site/data/`. No build step required. Client-side search via a prebuilt index
(MiniSearch or Lunr from a CDN). Charts via a CDN library (Chart.js or D3).

**Chat backend (Phase 6):** One Cloudflare Worker (TypeScript, Wrangler), modeled on
the existing `verra-v5-qa` project. It receives a question, retrieves the most relevant
content chunks, calls the Claude API, and returns an answer with citations. API key
stored as a Worker secret. Rate limited.

**Hosting (proposed, confirm in Q2):** Cloudflare Pages for the static site, the Worker
on the same account. Alternative: Vercel or GitHub Pages plus a Worker for chat.

**Data flow:**
```
sourcedoc/*.pdf  -->  content/raw/<src-id>/   (Markdown + images + page_map.json)
                            |
                            v   (agent reads in full, synthesizes)
                      content/synth/<src-id>.md   (structured notes with page refs)
                            |
                            v
                      site/data/*.json   (insights, vendors, stats, glossary, sources, chunks)
                            |
                 +----------+-----------+
                 v                      v
           site/*.html (pages)    worker/ (chat, reads chunks.json)
```

**Planned directory layout:**
```
GenAI_Market/
  CLAUDE.md, plan.md
  sourcedoc/                # input PDFs (never edited)
  scripts/                  # extract_pdf.py, build_search_index.py, validate_data.py
  content/
    raw/<src-id>/           # extracted markdown, images, page_map.json
    synth/<src-id>.md       # agent-written structured synthesis with page citations
    schemas.md              # JSON record shapes for site/data
  site/
    index.html              # dashboard / landing
    insights.html           # key findings, challenges, trends
    vendors.html            # table + quadrant chart + profile drawer
    stats.html              # survey statistics and charts
    glossary.html
    sources.html            # bibliography and methodology
    chat.html               # ask-the-report (or a panel on every page)
    assets/  css/  js/
    data/                   # insights.json, vendors.json, stats.json, glossary.json,
                            # sources.json, taxonomy.json, chunks.json, search-index.json
  worker/                   # Cloudflare Worker for chat (wrangler.toml, src/index.ts)
```

**Content and citation rules (meta-analysis standard):**
- Every insight, stat, and vendor fact carries one or more references: `source_id` plus `page` (or `url`).
- All text on the site is written by us, in our own words. No verbatim passages from any source. For SRC-01 this is absolute: not even short quotes.
- Findings are stated as research outcomes ("Analyst and survey sources converge on...", "AIM Research (2026) places X in...") not as reproductions.
- Where two or more sources support a finding, cite all of them. Where sources disagree, present both positions.
- Vendor entries record factual positioning (quadrant, focus areas, notable strengths) in our words. No analyst prose is copied.
- No source images or charts are reused. All charts are rebuilt from `stats.json`.
- The Sources page carries a full bibliography and a methodology statement describing the meta-analysis approach.
- `content/schemas.md` defines each JSON record shape so agents produce consistent data.

---

## 5. Implementation Steps

Legend: `[ ]` not started · `[~]` in progress · `[x]` done (add date and a short note)

Format example:
`- [x] 0.1 Create CLAUDE.md — 2026-09-15, agent instructions for reading/updating this plan (CLAUDE.md)`

### Phase 0 — Project scaffolding
- [x] 0.1 Create `CLAUDE.md` — 2026-09-15, agent instructions for reading and maintaining this plan (`CLAUDE.md`)
- [x] 0.2 Create `plan.md` skeleton — 2026-09-15, this file with all sections and placeholders (`plan.md`)

### Phase 1 — Define scope
- [x] 1.1 Define project scope with user — 2026-09-15, objective, audience (public), stack (static + JSON), features (search, glossary, vendors, insights, chat), research scope (PDFs + web). Sections 1 to 4 filled in.

### Phase 2 — Source ingestion
- [x] 2.1 Write `scripts/extract_pdf.py` — 2026-09-15, PyMuPDF only (docling not needed). Outputs `text.md` (page-delimited, boilerplate stripped), `tables.md`, `page_map.json` (TOC), `meta.json`, and `pages/NNN.png` renders at 110 dpi instead of embedded images, because page renders are what an agent needs to read charts (`scripts/extract_pdf.py`)
- [x] 2.2 Run extraction on SRC-01 and SRC-02 — 2026-09-15, src-01: 90 pages, 436 TOC entries, 36 tables, 162k chars. src-02: 28 pages, 18 TOC entries, 16 tables, 21k chars. Output in `content/raw/src-01/` and `content/raw/src-02/`
- [x] 2.3 Extraction QA — 2026-09-15, checked pages 8, 14, 15, 23 (src-02) and 25, 31 (src-01). Fixed one bug: boilerplate filter was stripping the IDC tier labels (Leaders, Advanced, Developing, Emerging) because they recur on most pages; now lines under 20 chars are never treated as boilerplate. Known limitation: multi-column tables (IDC appendix p23-25, AIM tables) scramble in `text.md` and only partly detect in `tables.md`. The `pages/*.png` renders are clean and are the reference for tables and charts. Phase 3 must read those renders for any numeric table.

### Phase 3 — Content synthesis (read everything, write structured notes)
- [x] 3.1 Read SRC-01 in full and write `content/synth/src-01.md` — 2026-09-15, all 90 pages read (text plus renders of p28 quadrant and p29 score table). Notes contain: document facts and method, 15 market findings (F1 to F15), 6 challenges, 7-category service taxonomy, 5 workload types, industry use-case matrix, 6 scale-management practices, pricing benchmarks, 6 engagement models, delivery roles, full 28-vendor score and quadrant table, 28 vendor notes (V01 to V28), 10 cross-vendor patterns (P1 to P10), glossary term list, 20 numeric stats. Quadrant boundary inferred at 0.5/0.5. Source table header says "Rank 2024" but it is the 2026 edition. (`content/synth/src-01.md`)
- [x] 3.2 Read SRC-02 in full and write `content/synth/src-02.md` — 2026-09-15, all 28 pages read (text, appendix tables p23-25, renders of p4, p5, p6, p12 to verify chart values). Notes contain: document facts and sponsor-bias note, 4-tier maturity model with n and scores, 17 findings (F1 to F17) with every chart's numbers, IDC's 5 recommendations, 8 cross-source observations (O1 to O8) linking to SRC-01, glossary list, 31 numeric stats. Source inconsistencies noted: Developing n given as 195 and 155 on different pages; success mean based on n=815. (`content/synth/src-02.md`)
- [x] 3.3 Write `content/schemas.md` — 2026-09-15, record shapes and id conventions for all seven data files, ref format for PDF and web sources (`content/schemas.md`)
- [x] 3.4 Produce `site/data/sources.json` and `site/data/glossary.json` — 2026-09-15, 2 sources with method, licence and bias notes; 98 glossary terms with definitions in our words, tags, cross-links and refs
- [x] 3.5 Produce `site/data/insights.json` — 2026-09-15, 40 insights across 12 themes (findings, observations, recommendations), each with summary, detail, evidence level, refs, and links to stats, terms and vendors. 12 are multi-source, 7 are agent-analysis.
- [x] 3.6 Produce `site/data/vendors.json` and `site/data/taxonomy.json` — 2026-09-15, 28 vendors with scores, ranks, quadrant, focus, industries, markets, named platforms, differentiators, claimed metrics, partners, service categories; taxonomy holds 7 service categories, 5 workloads, 13 industry use-case rows, 6 engagement models, 6 scale practices, 4 maturity tiers
- [x] 3.7 Produce `site/data/stats.json` — 2026-09-15, 39 stats (13 from AIM, 26 from IDC) with series for charting, sample notes and caveats
- [x] 3.8 Produce `site/data/chunks.json` — 2026-09-15, generated by `scripts/build_chunks.py` from the other files rather than hand-written, so it stays in sync. 248 chunks, longest 247 words, each with source ids, pages and a site anchor.
- [x] 3.9 Write `scripts/validate_data.py` — 2026-09-15, checks parse, unique ids, refs resolve to sources and page ranges, cross-links resolve, quadrant matches scores, multi-source insights cite two sources, units and chart types. First run: 0 errors, 0 warnings.

### Phase 4 — Web research
Phase 4 working rule: one step at a time, with a user checkpoint after 4.1. Many market reports are gated behind a sign-in form. The agent does not attempt to bypass gates. It lists the candidates and marks each as **open** (agent can fetch the full document), **gated** (user signs in, downloads the PDF, and saves it to `sourcedoc/`), or **summary-only** (only a public summary page exists; usable with a lower evidence weight). The user reviews the list, downloads what they can, and says which to proceed with.

- [x] 4.1 Identify 5 to 10 reputable 2025 to 2026 sources — 2026-09-15, 11 candidates checked for access: 7 open (Deloitte, BCG, Menlo, a16z, KPMG, Microsoft, Stanford), 2 need the user (McKinsey blocks automated requests; MIT NANDA is behind an email form), 2 summary-only (IDC FutureScape, Gartner). BCG PDF already saved to `sourcedoc/bcg-ai-radar-2026.pdf`. Deliverable: `content/web-candidates.md` with proposed ids src-03 to src-13 and a processing order.
- [x] 4.1b User checkpoint — 2026-09-15, user downloaded McKinsey (31 pages, August 2026 edition) and MIT NANDA (26 pages) into `sourcedoc/`. Agent downloaded Deloitte (41 p), KPMG (8 p), Microsoft (33 p), Stanford (425 p) and saved Menlo, a16z and IDC blog pages as HTML. All 10 new sources registered in `sources.json` (src-03 to src-12). `extract_pdf.py` now supports page ranges and skips missing files; new `scripts/extract_web.py` converts saved HTML to Markdown with trafilatura. Extraction run on all seven new PDFs (Stanford: Top Takeaways, Economy and Public Opinion chapters only, 88 pages) and three web pages. Gartner skipped.
- [x] 4.2 Synthesize each confirmed source into `content/synth/src-NN.md` — 2026-09-15, all ten new sources done (src-03 to src-12); twelve synth files in total. Per-source progress:
  - [x] src-05 BCG AI Radar 2026 — 2026-09-15, 29 pages read, 7 chart renders checked. 19 findings, 7 cross-source observations, 30 stats. (`content/synth/src-05.md`)
  - [x] src-04 Deloitte State of AI in the Enterprise 2026 — 2026-09-15, 41 pages read, 8 chart renders checked (p10, p14, p15, p17, p22, p23, p25, p28). 26 findings, 9 cross-source observations, 35 stats. Source inconsistencies logged (N typo p11, wrong Americas N on p23). (`content/synth/src-04.md`)
  - [x] src-09 KPMG AI Pulse Q2 2026 — 2026-09-15, 8 pages read, all 5 content-page renders checked (p3 to p7; text extraction mispaired most values). 17 findings, 8 cross-source observations, 22 stats. Q1 stage breakdown does not reproduce the stated Q1 deployment rate; logged. (`content/synth/src-09.md`)
  - [x] src-07 Menlo Ventures 2025 (web) — 2026-09-15, full article read plus all 14 data charts (fetched to scratchpad, converted to PNG, read, discarded; not stored in project). 20 findings, 10 cross-source observations, 33 stats. Prose/chart mismatch on departmental categories logged. Strong bias note: Menlo is an Anthropic investor, so the LLM-share finding is flagged. (`content/synth/src-07.md`)
  - [x] src-08 a16z CIO survey 2026 (web) — 2026-09-15, full article read plus all 12 data charts (fetched to scratchpad, read, discarded). 16 findings, 9 cross-source observations, 23 stats. Prose/chart mismatch on the LLM spend baseline logged ($4.5M was a projection, not the 2024 actual). a16z is an OpenAI investor; the OpenAI-leads finding is flagged and reconciled with Menlo's Anthropic-leads finding in O1. (`content/synth/src-08.md`)
  - [x] src-10 Microsoft Work Trend Index 2026 (33 p) — 2026-09-15, forked agent; all 33 pages read, renders checked p8, p12, p16, p18. 24 findings, 9 observations, 31 stats. Inconsistencies logged (two fieldwork end dates, 29 vs 27 markets, two "Frontier" constructs). Vendor telemetry and correlational factor analysis flagged. (`content/synth/src-10.md`)
  - [x] src-11 Stanford AI Index 2026 (88 selected pages) — 2026-09-15, forked agent; Top Takeaways, Economy and Public Opinion chapters read, 16 renders checked. 45 findings, 11 observations, 57 stats. Every finding names the underlying data source; the adoption and agent charts are McKinsey 2025 and must not be double counted against src-03. Three inconsistencies logged. (`content/synth/src-11.md`)
  - [x] src-03 McKinsey State of AI 2026 (31 p) — 2026-09-15, forked agent; all 31 pages read, every exhibit page rendered and checked (20 renders). 26 findings, 12 observations, 35 stats. Six exhibit-labelling inconsistencies logged. Only source with a before-and-after workforce check (32% expected cuts, 14% reported). (`content/synth/src-03.md`)
  - [x] src-06 MIT NANDA GenAI Divide 2025 (26 p) — 2026-09-15, forked agent; all 26 pages read, 10 renders checked. 28 findings, 10 observations, 26 stats. Document-facts table records exactly what the "95% fail" figure measures (custom task-specific tools reaching sustained impact within six months, 52-organisation interview sample). Five inconsistencies logged; the authors' protocol agenda flagged. (`content/synth/src-06.md`)
  - [x] src-12 IDC FutureScape 2026 summary (web) — 2026-09-15, public blog read in full; no charts. 6 findings (3 numeric predictions), 5 observations, 4 stats. Lowest evidence weight; outlook use only. (`content/synth/src-12.md`)
- [x] 4.3 Merge web findings into insights, stats, glossary, and chunks — 2026-09-15. stats.json 39 to 259 (stat ids keep the synth numbering, e.g. synth S4-02 is stat s4-02); glossary.json 98 to 164 terms plus new refs on 19 existing terms; insights.json 40 to 84 (ins-041 to ins-084: 44 new, of which 30 multi-source), and 13 existing insights given new refs, related stats and where justified raised to multi-source (ins-005, 007, 010, 013, 018). Web refs carry the source url; PDF refs carry pages. Merge scripts kept in the session scratchpad only; the JSON files are the record. validate_data.py: 0 errors, 0 warnings. build_chunks.py: 599 chunks, longest 348 words.
- [x] 4.4 Write a "Future outlook" insight set that reconciles the sources — 2026-09-15, ins-085 to ins-095 (theme outlook): spend pace, agents on paper vs governed agents, organisational constraint and leading-group size, ROI J-curve, cost discipline, buy-vs-build bifurcation, workforce pace, governance and sovereignty divergence, physical AI, a disagreement scorecard (vendor leader, size, failure rate, spend pace, jobs) and eight indicators to watch in the 2027 editions. Each states where sources agree and where they differ, with refs to every source drawn on.

### Phase 5 — Website build
Target: MVP deployed on GitHub Pages from https://github.com/rbndchsn/genai_market (public repo). No chat in the MVP; Phase 6 is paused.
- [x] 5.0 Repository setup — 2026-09-15, `git init -b main`; `.gitignore` excludes `sourcedoc/`, `content/raw/`, `content/synth/` (Q6: keep local for now), `content/originality-check-*.md` (quotes source runs), Python and OS files; `.gitattributes` normalises line endings; `README.md` stub. Verified with `git ls-files` that no PDF, render or extraction is tracked (19 files: plan, CLAUDE.md, README, schemas, web-candidates, 5 scripts, 7 data files, git config). First commit a4d555c pushed to https://github.com/rbndchsn/genai_market on `main`.
- [x] 5.1 Site shell — 2026-09-16. `site/css/site.css` (colour, spacing, radius and font tokens on `:root`; dark theme under `prefers-color-scheme` guarded by `:root:not([data-theme="light"])` and again under `:root[data-theme="dark"]`; sticky header; nav that scrolls horizontally on phones; card, pill, notice, table and form primitives). `site/js/shell.js` renders the skip link, header, nav (active item from `<body data-page>`), footer and a theme toggle stored in localStorage, and exposes `window.GM` with `loadJSON`, `escapeHTML` and `formatRefs` for later pages. `site/assets/favicon.svg`. Seven pages generated with a shared head and placeholder bodies: index, insights, vendors, stats, glossary, sources, 404. All paths relative. Verified with headless Chrome: desktop, dark, light (stored preference) and a 390px iframe harness (desktop Chrome cannot shrink below about 500px, so phone width is tested through an iframe); no horizontal overflow. Fixed three defects found in review: card links inheriting underlines, card spacing rule firing inside the grid, toggle icon crushed by generic button padding.
- [x] 5.2 `index.html` dashboard — 2026-09-16. `site/js/home.js` reads every number from `stats.json` (hero figure s7-02; eight stat tiles s3-02, s4-02, s3-15, s3-06, s4-25, s9-10, s5-08, s7-09), renders eight curated top insights from `insights.json` (ins-050, 054, 046, 053, 072, 065, 085, 094) with theme and evidence pills and references, a counts line computed from the data files, section links, and a strip of the twelve sources ordered by date. Followed the `dataviz` guidance: one hero figure, KPI row of stat tiles, no chart on this page; chart colour tokens (`--series-1..8`, sequential and diverging steps) added to `site.css` after validating the reference palette against our light (#ffffff) and dark (#1d2025) surfaces with the palette checker (all checks pass; three light slots need direct labels or a table view, noted for 5.5). `shell.js` gained short publisher names for citations. Fixed one defect in review: nested anchors in stat tiles ejected the reference links. Reviewed with headless Chrome at desktop and 390px.
- [x] 5.3 `insights.html` — 2026-09-16. `site/js/insights.js`: filters by theme, evidence level, source and free text, synced to the query string (`?theme=agentic&evidence=multi-source` is shareable); cards grouped under theme headings with counts when no theme is selected; each card shows theme, evidence, kind and id pills, the summary, and a native `<details>` block with the full detail, formatted references, and chips linking related statistics (`stats.html#id`), glossary terms and vendors. Deep links `#ins-NNN` open, highlight and scroll to the card and override filters. Verified with headless Chrome: unfiltered desktop view, filtered phone view (5 of 95), deep link opens the target (DOM check) and the expanded layout. Fixed: sticky header covering anchored cards (global `scroll-padding-top`), pill links inheriting underlines.
- [x] 5.4 `vendors.html` — 2026-09-16. `site/js/vendors.js`: method card rendered from `vendors.json` meta (axes, note, refs); filters by quadrant, HQ country, service category (taxonomy) and free text, mirrored in the query string together with the sort column and direction (`?quadrant=seasoned&country=India`, `?sort=ma_score&dir=desc`); PeMa quadrant as an SVG scatter (28 points, threshold 0.5 lines, quadrant captions, one hue with de-emphasised grey for filtered-out vendors, direct labels for Leaders or for any filtered set of 12 or fewer with a collision pass and leader lines, 14px hit radius, hover and focus tooltip, click or Enter opens the profile, re-rendered on resize; on phones under 560px labels are dropped unless six or fewer vendors are shown, and the table carries the names); legend pills with per-quadrant counts that toggle the quadrant filter, plus the quadrant meanings; sortable table (aria-sort, nine columns, stable `id` per row) with vendor names as buttons; profile drawer as a native `<dialog>` (focus moves to close, Escape and backdrop close, focus returns to the opener) showing focus, named platforms, differentiators, claimed metrics under a vendor-claimed-unaudited pill, industries, markets, service-category chips, partners, related insights and the page refs. Deep link `#blend360` clears filters, highlights and scrolls to the row and opens the drawer. All 28 vendors covered (Q5). Verified with `review_shots.py`: desktop and phone, filtered dark view, deep link in light theme (through the new `--theme` and `--iframe` options), DOM checks (28 rows with ids, 10 labels, dialog open on hash, aria-sort). Fixed: vendor labels colliding with each other and with quadrant captions on phones (captions for the high-maturity quadrants now sit above the plot; labels dropped on narrow screens); headless Chrome paints a top-level page blank while a modal dialog is open, so `review_shots.py` gained `--iframe` (desktop through a same-origin iframe) and `--theme light|dark` (stores the theme preference before load).
- [x] 5.5 `stats.html` — 2026-09-16. `site/js/stats.js` renders all 259 records grouped under the twelve theme headings, one card per record with theme, source and id pills, the chart, a `Data table` twin (`<details>`), sample, note, formatted refs and `Used in` chips to the insights that cite it. Forms follow the `chart` hint: `bar` as HTML horizontal bars (one hue, 18px, 4px rounded data-end, value at the tip; lists over 12 collapse behind `Show all N`, used by 10 records including s1-09 and s1-10); `grouped-bar` parses `Category, Group` / `Category: Group` / `Category 2025` / `Category now` labels, makes the smaller set the colour series in fixed slot order (max 8), draws a legend and 12px bars with 2px gaps, values at tips only for three or fewer series (50 records; four with inconsistent label wording fall back to flat bars: s5-19, s3-32, s11-14, s11-38); `donut` as an SVG ring with 2px gaps and a stacked legend with values (17 records; s4-32 with seven slices falls back to bars); `range` as a band on a track, log scale when the span exceeds 20x (s1-05); single values as stat tiles or text; s1-16 as prose. Every mark has a hover and focus tooltip (values lead, labels follow, textContent only). Filters: theme, source, form (charts only, bar, grouped, donut, range, single figures) and search, mirrored in the query string; deep links `#s4-35` clear filters, highlight the card, open its table and scroll to it. Verified with `review_shots.py`: vendors theme (desktop and phone), Menlo source in dark, deep link in light; DOM check counted 259 cards, 50 grouped legends, 17 donuts, 3 ranges, 50 tiles, 206 data tables. Fixed: score values now show two decimals (0.90 not 0.9), phone bar values aligned to the bar, `review_shots.py` DOM dump now decodes UTF-8.
- [x] 5.6 `glossary.html` — 2026-09-16. `site/js/glossary.js`: 164 terms sorted alphabetically under letter headings with counts, a letter index (letters without terms greyed out), filters by tag (with counts) and free text over term, expansion and definition, mirrored in the query string (`?tag=governance`); each entry carries its `id`, term, expansion, definition, tag pills linking to the tag filter, related-term chips (same page), `Used in` chips to the insights whose `related_terms` include it, and formatted refs. Deep link `#sovereign-ai` clears filters, highlights and scrolls to the term. Verified with `review_shots.py`: light desktop and phone (unfiltered), dark desktop and phone with `?tag=governance#sovereign-ai` (filter cleared by the hash, target highlighted). No defects found.
- [x] 5.7 `sources.html` — 2026-09-16. Static sections (on-page nav, Method with the six-step pipeline and what we did not do, Evidence levels, Licensing and attribution, How to cite, Data files) plus `site/js/sources.js`, which renders the bibliography from `sources.json` ordered by date: short-name, type and sponsor pills, title, publisher, authors, month and year, page count, accessed date and link, then Method, Sample, How we use it, Caveats and Licence fields, and an `On this site` line counting the insights, statistics, glossary terms and vendor profiles that cite the source (links to the filtered Insights and Statistics views). Evidence-level cards show computed counts; the citation example is built from the first insight and the page URL; the data-files list links every JSON file. Deep links `#src-04` highlight and scroll; section anchors `#method`, `#cite` work. Verified with `review_shots.py` in light (top and `#cite`) and dark (`#method`), desktop and phone. Fixed: two licence notes in `sources.json` said "by the user" (internal phrasing) and now say "by the authors"; `validate_data.py` 0 errors, `build_chunks.py` re-run (599 chunks).
- [x] 5.8 Search — 2026-09-16. `scripts/build_search_index.py` writes `site/data/search-index.json` (599 docs from `chunks.json` with title, type, url, 220-char snippet and source ids; a 3,807-token inverted index of 31,849 postings, 379 KB; glossary acronym expansions indexed with their terms; light suffix stripping and a stop list mirrored in the client). No CDN library: `site/js/search.js` normalises the query the same way, matches token prefixes by binary search over the sorted key list, requires every word to match, scores by inverse document frequency with a title boost and a small glossary and insight boost, and renders results with type and id pills, highlighted title and snippet and the short source names; type pills filter the result set; `?q=` and `?type=` live in the URL; an empty query shows starting-point searches and section links. `search.html` added; `shell.js` gained a Search nav item and a header search form (hidden under 900px, and on the search page itself). Verified with `review_shots.py`: `?q=agent governance` light desktop and phone (35 results, types counted), empty dark, and the home page header with the search box. Run `build_search_index.py` after `build_chunks.py` on every data change.
- [x] 5.9 Local run and review — 2026-09-16. New `scripts/check_site.py` serves `site/`, renders every page with headless Chrome (scripts run, data loaded) and checks console errors, that every internal href resolves to a file, that every hash anchor is an id the target page renders, and that no path is absolute: 8 pages, 1,333 internal links, 0 console errors, 0 problems. Screenshot sweep with `review_shots.py` of all eight pages (index, insights, vendors, stats, glossary, sources, search, 404) in light and dark at desktop and 390px; reviewed, no overflow or collisions. Fixed: `404.html` now anchors relative URLs to the project root on github.io (Pages serves it for nested missing paths) and links to search; `index.html` Explore section gained a Search card.
- [x] 5.10 Deploy to GitHub Pages — 2026-09-16. `.github/workflows/pages.yml` uploads `site/` as the Pages artifact and deploys on every push to `main` (also on manual dispatch); Pages enabled through the API with `build_type=workflow` (decided: Actions workflow from `site/`, no `docs/` copy). First run 35062535196 succeeded. **Live URL: https://rbndchsn.github.io/genai_market/**. Verified: every page, the data files, CSS and JS return 200; headless Chrome renders index (8 stat tiles), `vendors.html#blend360` (drawer open), `stats.html?theme=vendors` (25 cards) and glossary (164 terms) from the live site with 0 console errors; a nested missing path returns the 404 page with its base anchored to the project root. MVP launched.

### Phase 6 — Ask-the-report chat (PAUSED, user decision 2026-09-15)
Paused so the MVP does not depend on a Cloudflare Worker or an API key. Resume after the static site is live and reviewed. Steps kept for later:
- [ ] 6.1 Scaffold `worker/` with Wrangler, modeled on `verra-v5-qa`
- [ ] 6.2 Retrieval — embed or keyword-score `chunks.json`, return top chunks with citations
- [ ] 6.3 Claude API call with a system prompt that restricts answers to retrieved content and requires citations (load the `claude-api` skill before writing this)
- [ ] 6.4 Rate limiting, CORS, input length limits, API key as a Worker secret
- [ ] 6.5 Chat UI — panel or page, shows the answer plus cited chunks, handles errors and empty results
- [ ] 6.6 Test with 20 representative questions, record answers, fix retrieval gaps

### Phase 7 — QA and launch
- [x] 7.0 Visual refresh of the colour scheme — 2026-09-16, user request after launch (pure white cards and near-black text were hard on the eyes). `site.css` tokens only, so every page changed at once. Light: page #f4f1ea with a soft ivory gradient at the top, cards #fbf9f4, secondary surface #ece7dc, text #2c3340 (charcoal), muted #5e646f, teal accent and links #1d6b6b, amber focus #d99a1e. Dark: page #1a1d23, cards #22262e, text #e6e2d9 (warm off-white), muted #a29e95, light teal accent #7cc4bf. Chart chrome tokens (grid, axis, de-emphasis, diverging midpoint) retuned to the new surfaces; the eight series colours are unchanged and were re-validated with the dataviz palette checker against #fbf9f4 and #22262e (all checks pass; the same three light slots rely on the table view, which every chart has). Text contrast computed for every pairing: lowest is muted text on the secondary surface at 4.6:1 light and 5.0:1 dark, everything else 5:1 to 13:1. Reviewed index, vendors, stats, insights and glossary in both themes at desktop and phone. Committed and pushed; Pages republished.
- [x] 7.0b Colour with a job — 2026-09-16, user asked how to bring colour to a site that read as monochrome; agent recommended a bundle and the user said go. (1) Theme colours: `shell.js` maps the eight large themes to fixed chart-palette slots (market blue, adoption green, agentic orange, data dark green, governance violet, talent pink, vendors amber, outlook teal accent) and the four small themes (cost, operations, commercial, method) to a neutral; never cycled. `GM.themeStyle(theme)` sets `--tc` inline and CSS paints a 4px left edge on insight and stat cards, a top rule on home stat tiles, a tinted theme pill with a coloured dot (text stays in text tokens) and a square before each theme heading. (2) Home hero band: teal-to-navy gradient with light text (darker gradient in dark mode). (3) Evidence pills: green (multi-source), amber (single source), violet (our analysis) via `GM.evidenceClass`. (4) Vendor quadrant tints: Leaders teal, Seasoned violet, Growth amber, Challengers neutral as soft washes behind the plot, with matching pills in the legend, table and drawer; points stay one hue. Stat-tile values now in the accent colour. Reviewed home (light and dark, desktop and phone), insights, stats (dark) and vendors; `check_site.py` clean. Header tint (item 6) added on the user's go: faint teal wash behind the header, teal-tinted bottom rule and nav hover.
- [x] 7.1 Originality review — 2026-09-16 (SRC-01 part done; the licensed source is clean). Re-ran `originality_check.py`: 30 records with 110 eight-word runs matched the AIM extraction. All rewritten in our own words in `insights.json` (20 insights: ins-001, 002, 004, 005, 006, 008, 009, 010, 011, 012, 013, 014, 015, 016, 023, 029, 030, 036, 037, 039), `glossary.json` (hallucination, multi-agent-orchestration), `stats.json` (s1-16 note) and `vendors.json` (platform descriptions, differentiators and focus text for ten vendors; industry, partner and market lists re-ordered alphabetically where the original order alone produced a match, since the names themselves are facts). A stricter check over every vendor field now finds zero SRC-01 runs; the report file lists no SRC-01 records. `validate_data.py` 0 errors; chunks and search index rebuilt; `check_site.py` clean. Remaining overlap with the public sources (30 records, 72 runs; largest src-02 with 43 runs, mostly chart labels and survey question wording) is not a licence issue and is left for a follow-up pass. Original head-start note: `scripts/originality_check.py` runs the 8-word shingle check of insights, glossary, stats and vendors against every raw extraction and writes `content/originality-check-<date>.md`. First run: 59 records with at least one 8-word run, of which 30 records (110 runs) match SRC-01 and 16 (43 runs) match SRC-02, almost all from the Phase 3 content; the Phase 4 additions account for 14 records with 1 to 9 runs each, mostly chart labels and question wording. All SRC-01 hits must be rewritten.
- [x] 7.0c Tag sphere on the Search page — 2026-09-16, user request modelled on a tag globe from another of their tools (reference screenshots in the git-ignored `assets/` folder). `site/data/search-tags.json` holds 72 curated queries (agents and governance, pilot to production, cost and ROI, workforce and leadership, risk and trust, data and RAG, vendors and the PeMa quadrant, regions, industries, outlook). `search.js` runs each tag through the search at load time, drops any with fewer than three results (none dropped today), sizes tags by log result count, lays them on a Fibonacci sphere projected onto an ellipse that fills the card (horizontal radius from the widest label so nothing clips), turns it slowly, steers with the pointer, pauses on hover or focus, stops for reduced motion, and shows only the 32 strongest tags under 560px. Every tag is a real button with a title giving its record count; clicking runs the search in place. Hero gradient reused as the card background in both themes. `content/schemas.md` documents the new file; `.gitignore` now excludes `/assets/`. Reviewed light and dark at desktop and phone; DOM check found 72 tags at 12 to 24px; `check_site.py` clean. Fixed after first review: sphere was capped by the card height and bunched in the middle third, and 72 tags were unreadable on a phone. Second pass after the user saw it live (tags slid along fixed rows, no sphere visible, and stuttered under the pointer): rotation now tumbles about a tilted axis, the projection is a circle with perspective (front tags spread outward, back tags pull in, rim tags at the edge; radius sized from the rim labels), hover eases the spin to a stop instead of toggling it, and the card is 600px tall on desktop with 60 tags and 380px on phones with 26.
- [x] 7.0d "How it was built" page — 2026-09-16, user asked for an educational write-up for entry-level and seasoned developers. `site/method.html`: the setup (VS Code, Claude Code, Claude Fable 5.1, division of labour), the two coordination files and why a plan beats a prompt, the eight phases at a high level, the working rhythm and checkpoints as a dial, the content rules, engineering choices, three bugs and their lessons, takeaways for both audiences, links to the repository, plan and agent instructions. Scale tiles read from the data files (`site/js/method.js`). Nav item and footer links in `shell.js`, styles `.takeaways`, `.takeaway`, `.phases` in `site.css`. Reviewed light desktop and phone, dark takeaways; `check_site.py` 9 pages clean. Fixed before review: dropped a page-count tile (Stanford was read in part) and made the reading claim exact. Fixed after review: the longer nav label pushed the Search item off the edge of the horizontally scrolling nav at desktop width, so nav links now carry `data-key` and the Search item is hidden above 900px except on the Search page, where the header search box is absent.
- [x] 7.2 Content accuracy evaluation — 2026-09-16, 20 records drawn with seed 20260916 (12 stats, 8 non-agent insights) and checked by four parallel general-purpose agents against the page renders, extracted text and saved articles (two Menlo chart images re-fetched to the scratchpad). Result: 12 match, 6 minor, 2 wrong. Corrected in the data: s11-57 note (every state with reported data); ins-041 (a16z application spend was written as $5.8B per firm, source says about $5.8M average; baseline made explicit); ins-067 (BCG figure is the full executive sample, McKinsey page measures risks mitigated); ins-064 (IDC 40% is a tie for first, AIM is a provider sample, src-02 p.15 added, title shortened); ins-065 (only Menlo names Jevons); ins-080 (J-curve is Brynjolfsson's explanation relayed by the Index, METR non-replication added, national evidence early rather than small); ins-063 (chat-tool skills lost share while counts rose, decade-versus-year growth noted, src-01 p.5 added, evidence lowered to single-source). ins-083 left as is: three a16z figures are chart readings from synthesis time and the a16z chart images are not kept, disclosed on the site. validate_data 0/0, chunks and index rebuilt, originality check still zero SRC-01 hits. Results published: `site/data/quality.json` (schema in `content/schemas.md`) rendered as a Validation and evaluation section on `method.html` with the three layers named (validation, originality check, accuracy evaluation), verdict tiles and a per-record table. Next evaluation should use a new seed.
- [x] 7.2b Full accuracy evaluation — 2026-09-16. The 7.2 sample (8 of 20 needing a fix) could not bound the error rate, so the evidence layer was checked in full. Scope: all 95 insights (79 sourced, 16 our analysis), all 28 vendor profiles, all 259 statistics (a 40-record sample drawn with seed 20260917 found a wrong value, which triggered the check of the remaining 207) and 20 glossary terms (seed 20260917); 402 records in all. Method: `accuracy-eval` workflow, checkers on Claude Opus 5 (a different model from the author model), a structured verdict per record; insights and vendors read twice, the second pass adversarial; calibration first on the eight original 7.2 insights (caught both known errors and all minor ones, and three problems 7.2 had missed); every wrong verdict confirmed by the main session against the page render or text before data changed (two downgraded to minor, ins-008 and ins-064; one correction kept for a different reason, ins-050). Result: 236 match, 133 minor, 33 wrong (insights 8 match, 62 minor, 25 wrong; vendors 15/13/0; statistics 199/52/8; glossary 14/6/0). The second pass found problems in 15 of 123 records the first had passed. Wrong statistics were mostly chart readings, including an a16z bar chart whose tick labels are offset from its gridlines (s8-02 re-measured from the baseline; local synth notes corrected too). Insight errors were mostly misplaced true figures: wrong survey, wrong population, share read as count, forecast as actual, tie as lead, agreement claimed across sources that only one source makes. Eight insights now single-source (7 today plus ins-063 in 7.2). Fixes: 161 adjudicated edits applied by `apply_edits.py` (scratchpad), plus 78 insight revisions drafted by an `apply-eval-fixes` workflow from the checkers' issues and reviewed diff by diff (one repair: merged a split ref). Three new eight-word runs against the licensed report, introduced by the corrections, were rewritten; originality check back to zero SRC-01 hits; public-source overlap now 87 runs in 34 records. validate_data 0/0, chunks and search index rebuilt, check_site 9 pages, 1,516 links, 0 errors. Published: `site/data/quality.json` now has rounds, per-layer coverage and one plain-language note for each of the 166 corrected records (notes drafted by a `write-correction-notes` workflow, all read, four rewritten); `method.html` shows a coverage table and a stacked correction list on phones, updated findings, a fourth lesson (the offset chart axis), the Opus 5 checker, and corrected takeaways. Reviewed desktop and phone, light and dark. Remaining: 144 glossary terms unchecked.
- [ ] 7.2c Accuracy check of the 144 glossary terms not in the 7.2b sample — same method as 7.2b (different-model checkers against the cited pages, adjudication before edits, originality re-scan), then update the glossary row and notes in `site/data/quality.json`. Tooling and procedure: `scripts/eval/` (README there). Not started; awaiting the user's decision. Optional (agent assessment, 2026-09-17): the 20-term sample had no wrong entries (14 match, 6 minor), so the escalation rule does not require a full check, and the method page already says the glossary was sampled. The only gain would be a "every record checked" claim, at the cost of another run the size of 7.2b; expect mostly loose wording.
- [x] 7.3 Technical QA — link check, Lighthouse (performance, accessibility), no console errors — 2026-09-17. Lighthouse 12.8 (installed in the session scratchpad, not the project) run on all nine live pages, mobile and desktop presets. Baseline: accessibility 100 except search 92 and method 98; best practices and SEO 100 everywhere; performance 69 to 86 on the data pages (404 and method 96 to 100), held back by layout shift (CLS 0.28 to 1.0). Causes found in the reports and with a height probe: the header was inserted after first paint (shift of 0.13 on every page); on desktop the scrollbar appeared when the data arrived and the whole page slid 7px sideways (0.85); containers filled by the page scripts pushed visible content and the footer down. Fixes: `shell.js` is now the first element of `<body>` on all nine pages and mounts the header at once, the footer after the body is parsed; `html { scrollbar-gutter: stable }`; data pages give `main` a 100vh minimum; containers that start empty reserve their measured height (412px and 1350px) only while `:empty`, so a filled container always takes its natural height and no gap can remain; long lists reserve 100vh. Accessibility: `role="group"` on the search type filter (aria-label on a plain div), sphere tags at least 24px, takeaway headings on the method page changed from h4 to h3 (heading order). Re-measured locally through a gzip server with Pages-like caching: every page 96 to 100 for performance with CLS 0, accessibility 100 except search 96, best practices and SEO 100. Accepted and not changed: search target-size (sphere tags overlap as they turn; every tag is also a query that can be typed in the search box, the equivalent the WCAG rule allows); the cache-lifetime warning (GitHub Pages fixes `max-age=600`); the DOM-size warning on stats (259 charts, TBT 20 to 210 ms). `check_site.py`: 9 pages, 0 console errors, all links and anchors resolve. Reviewed with `review_shots.py` (home desktop and phone, search dark desktop and phone, method) and a style probe of the takeaway headings. (`site/*.html`, `site/js/shell.js`, `site/css/site.css`)
- [ ] 7.4 Custom domain — register a domain for the site, add it to GitHub Pages (CNAME and DNS), and update absolute links in the README and on the method page. Depends on Q4 (site name). Redeploys already happen on every push to `main`, so no separate redeploy step is needed. A Worker deploy is needed only if Phase 6 resumes.
- [x] 7.5 Write `README.md` — how to run locally, how to add a new source — 2026-09-17. Most of the README already existed (layout, validate and rebuild, review, quality, plan). Added: requirements for running locally (any static server, not `file://`; Chrome or Edge for the check scripts; PyMuPDF and trafilatura only for extraction, verified that the other scripts import the standard library only) and an eight-step "Add a new source" section (get the document, register it in `sources.json` and the `SHORT` map in `shell.js`, extract, synthesise, merge, rebuild and check, update the fixed "twelve" wording, publish with the staging check). The synthesis procedure lives in the git-ignored `content/synth/README.md`, so the public README states the essentials itself. The places where "twelve" is typed were found by search: the `sources.html` meta description, the ins-094 summary and the README intro; the method page's "read twelve reports" describes the original build and stays. Fixed a wrong flag in the review example (`--dark` is `--theme dark`). (`README.md`)
- [x] 7.6 Licensing for reuse — 2026-09-17, files drafted: `LICENSE` (MIT, code only), `LICENSING.md` (path-to-licence table; exclusions: `vendors.json`, `taxonomy.json`, `chunks.json`, `search-index.json`, and the stats and insights whose refs cite only src-01, which today are the 13 `s1-*` statistics and 17 insights; glossary definitions stay licensed because they define general terms in our words even where AIM is cited, 69 of 164 cite only src-01; credit line; AI-assistance and not-legal-advice note), README licence section, Sources page "Our text and data" rewritten and a "Not licensed for reuse" paragraph added, footer link "Licence and reuse" (`shell.js`). `check_site.py` clean; Sources page reviewed on phone. Original step text:  — user decision 2026-09-17: allow reuse of code, text and data, commercial use included. Code under MIT (`LICENSE`); our own text and data under CC BY 4.0; material derived from the licensed AIM report (`vendors.json`, `taxonomy.json`) and the generated files that contain it (`chunks.json`, `search-index.json`) excluded, because the AIM licence terms are unknown. `LICENSING.md` maps paths to licences and gives the attribution line; README and the Sources page licensing section updated to match. Rights holder: Sustainable IQ (as already stated on the Sources page). Not legal advice; revisit if the AIM terms are found or a lawyer reviews it.

### Phase 8 — Maintenance and growth (later)
- [ ] 8.1 Add new PDF sources through the Phase 2 to 3 pipeline
- [ ] 8.2 Periodic web refresh of insights and stats
- [ ] 8.3 Privacy-friendly analytics to see which sections are used

---

## 6. Decisions Log

| Date | Decision | Reason |
|---|---|---|
| 2026-09-15 | Use `plan.md` + `CLAUDE.md` as the coordination mechanism between agents | Keeps state in the repo, readable by any agent, crossed off as work lands |
| 2026-09-15 | Website is public | User decision. Drives the synthesize-and-attribute content rule. |
| 2026-09-15 | Static HTML/JS + JSON data, no build step | User decision. Simple to host, easy for agents to generate content into. |
| 2026-09-15 | All four feature sets in scope: search + glossary, vendor explorer, insights + stats dashboard, ask-the-report chat | User decision |
| 2026-09-15 | Research scope: the two PDFs plus 5 to 10 targeted web sources in the first build | User decision |
| 2026-09-15 | Chat runs as a Cloudflare Worker, separate from the static site | Keeps the site static. Reuses the pattern already proven in `verra-v5-qa`. |
| 2026-09-15 | Synthesis happens offline (agents write JSON); only chat calls an LLM at runtime | Cost control, predictable content, no API key needed to view the site |
| 2026-09-15 | The site is a meta-analysis: original findings in our words, every finding referenced, no verbatim text from SRC-01 (AIM / Blend360), no reused images or charts | User decision. Avoids legal exposure from the licensed report while still using it as a cited input, the same way a literature review cites its sources. Closes Q1. |
| 2026-09-15 | Extraction renders every page to PNG rather than pulling embedded images. Page renders are the reference for tables and charts; `text.md` is the reference for prose. | PyMuPDF table detection misses multi-column layouts. Renders are reliable and readable by agents. Renders are working files only and never go on the site. |
| 2026-09-15 | `chunks.json` is generated from the other data files by a script, never hand-edited. | Keeps search and chat content in sync with the curated data. One place to change wording. |
| 2026-09-15 | Every insight carries an evidence level: single-source, multi-source, or agent-analysis. | Lets the site show readers how well supported each finding is, which is the point of a meta-analysis. |
| 2026-09-15 | Phase 4 runs one step at a time with a user checkpoint after the candidate list. The user retrieves gated PDFs and saves them to `sourcedoc/`; the agent never bypasses sign-in gates. | User decision. Market reports are often gated; the user has the accounts and can download the full documents, which are far better sources than public summaries. |
| 2026-09-15 | Vendor-share findings from the two venture firms (Menlo ranks Anthropic first, a16z ranks OpenAI first) are always presented together with their samples and each firm's investment in the vendor it ranks first. The site never declares a single leader. | Each firm has a conflict of interest and each measures a different population; the agreement on direction (Anthropic gaining, Google rising, open weights falling, multi-model the norm) is the reliable finding. |
| 2026-09-15 | Agent adoption and leading-group size are presented as ranges with the definition behind each source, not as single numbers. | Agent figures run from 7% to 59% and leading-group shares from 1% to 34% purely because of definitions (ins-050, ins-077). Quoting one number would misrepresent the evidence. |
| 2026-09-15 | Chart images from web sources are fetched into the session scratchpad for reading and never stored in the project. | Charts often hold numbers the prose omits; the no-source-images rule still applies to the repository and the site. |
| 2026-09-15 | Phase 6 (ask-the-report chat) is paused. The MVP is the static site only. | User decision. The chat needs a Cloudflare Worker and an API key, which would delay the MVP. |
| 2026-09-15 | Host on GitHub Pages from the public repo https://github.com/rbndchsn/genai_market; the project folder becomes a git repo. `sourcedoc/` and `content/raw/` are never committed. | User decision on hosting (closes Q2). The exclusions follow from the AIM licence and the repo being public. |
| 2026-09-15 | After src-08, the user asked for the rest of Phase 4 (remaining 4.2 sources, 4.3 merge, 4.4 outlook) to be completed in one run without per-source checkpoints. The four remaining PDF syntheses run in parallel as forked agents that write only their `content/synth/src-NN.md`; the main agent does src-12, updates `plan.md`, then does 4.3 and 4.4. | User decision, to finish Phase 4 without further interruptions. |
| 2026-09-16 | The user asked for the whole of Phase 5 to be finished in one run ("continue the plan.md and finish all phase 5"). Steps 5.4 to 5.10 run without per-step checkpoints; each step is committed and pushed when its review passes, because 5.10 (GitHub Pages) publishes from `main`. Open questions are settled with the recorded recommendations (Q5: all 28 vendors; Q4: working name GenAI Market). | User decision, to reach the MVP launch without further interruptions. |
| 2026-09-16 | Site headline is "A meta-analysis of the state of enterprise AI across twelve sources" (home heading, footer, citation line, README, home `<title>`). The user proposed "A Meta-Analysis of the State of Agentic AI Across Multiple Domains" and asked the agent to decide. Kept "meta-analysis" (accurate framing), kept "enterprise AI" rather than "agentic AI" because agentic AI is one of twelve themes (7 of 95 insights, 30 of 259 stats) and a narrower title would misdescribe most of the content, kept "twelve sources" over "multiple domains" because it is concrete and is the credibility signal of a meta-analysis. Agentic AI is named in the home page lead instead. The word "twelve" is typed in the heading, so it must be updated if sources are added. | User delegated the wording decision; accuracy of the headline against the content. |
| 2026-09-16 | Headline revised the same day at the user's request: "A Meta-Analysis of the State of Enterprise AI", in title case. The source count was dropped from the headline because it dates the site and must change whenever a source is added; the count still appears where it is computed from the data (home counts line, Sources page). Title case for the headline, browser title and citation title; the footer stays a sentence. | User feedback: the number added nothing to the title and titles should be capitalised. |
| 2026-09-16 | The Sources page does not list the JSON data files for download. The files stay in `site/data/` because every page reads them at run time. | User decision. No data licence has been written yet, two of the files are build artifacts, and the vendor records derive from a licensed report. If reuse is wanted later, add an explicit data licence line first. |
| 2026-09-16 | The Sources page has no "How to cite" section. Stable record ids and links stay. | User decision. Readers should cite the original source shown beside each finding; attribution for our own text is covered under Licensing. |
| 2026-09-16 | The site is show-only: no public issue tracker or corrections form. The author is credited in the footer with a LinkedIn link. | User decision. Corrections reach the author directly. |
| 2026-09-16 | The build method is published on purpose: a "How it was built" page on the site, links to it and to the repository in the footer and nav, and the plan, agent instructions, scripts and schema stay public in the repository. | User decision after weighing it: the method is the evidence of the AI skill the author wants to be known for; the site alone cannot show it. Copying the method does not copy the judgement or the maintenance. |
| 2026-09-16 | Three named checking layers, with numbers published from `quality.json`: validation, originality check, accuracy evaluation. An insight whose central figures rest on one source is single-source even when other sources corroborate a side remark (ins-063 downgraded). | Readers of a meta-analysis should see how it was checked; the words are chosen so that automated checks are not mistaken for evaluation. |
| 2026-09-16 | Repository settings: issues, wiki and projects switched off. The author remains the only collaborator; pull requests stay possible on GitHub but are proposals only. No branch protection while there is one writer. | Show-only project: readers may read and fork, and nothing an outsider does can appear under the repo's name. |
| 2026-09-16 | Accuracy is checked in full for every insight and vendor profile, sampled for statistics (escalating to all on any error) and glossary terms. Checkers are a different model from the author model and are calibrated on known errors first; a checker verdict never edits data without adjudication against the source. | User decision after the 7.2 sample found 8 of 20 records needing a fix. The sample was too small to bound the error rate; a meta-analysis cannot publish prose with an unknown error rate. User asked for the full run without per-fork checkpoints. |
| 2026-09-17 | The project is licensed for reuse, commercial use included: code under MIT, our own text and data under CC BY 4.0. Files derived from the AIM report (`vendors.json`, `taxonomy.json`) and the generated files that include them (`chunks.json`, `search-index.json`) are not licensed for reuse. The site stays show-only (no download links); the files are available from the repository. | User decision: the project should be free to use as well as to read. The AIM licence terms are unknown, so everything derived from that report is excluded to be safe. The show-only decisions of 2026-09-16 stand; `LICENSING.md` gives the attribution line in place of a "How to cite" section. |

---

## 7. Open Questions

- ~~**Q1.** AIM report licensing for public use.~~ Resolved 2026-09-15: meta-analysis framing, no verbatim text, no reused images. See Decisions Log.
- ~~**Q2.** Hosting: Cloudflare Pages (proposed) or Vercel / GitHub Pages? Custom domain?~~ Resolved 2026-09-15: GitHub Pages from https://github.com/rbndchsn/genai_market. Custom domain still open.
- **Q3.** Is an Anthropic API key available for the chat Worker, and is there a monthly budget cap? (Only relevant if Phase 6 resumes.)
- ~~**Q6.** Should `content/synth/` be committed to the public repo?~~ Resolved 2026-09-15: keep local for now (git-ignored). Revisit after 7.1.
- **Q4.** Site name and branding (working name: GenAI Market). Headline settled 2026-09-16 (see Decisions Log); the name itself is still the working name.
- ~~**Q5.** Should vendor profiles cover all vendors in the AIM report or only the top quadrant?~~ Resolved 2026-09-16: all 28 vendors appear in the quadrant, the table and the profile drawer. The filters let a reader narrow to Leaders.

---

## 8. Change History

| Date | Change |
|---|---|
| 2026-09-15 | Created plan skeleton. Phase 0 complete. |
| 2026-09-15 | Filled in sections 1 to 4 from scoping Q&A. Added Phases 2 to 8 with steps. Phase 1 complete. Added 5 open questions. |
| 2026-09-15 | Reframed the project as a meta-analysis. Tightened content rules: no verbatim text from SRC-01, no reused images or charts, every finding referenced. Reworded step 7.1 as an originality review. Closed Q1. |
| 2026-09-15 | Phase 2 complete. Added a Phase 3 reading guide under Current Status. Logged the page-render decision. |
| 2026-09-15 | Step 3.1 complete: SRC-01 synthesis notes written. |
| 2026-09-15 | Step 3.2 complete: SRC-02 synthesis notes written. Both PDFs fully read. |
| 2026-09-15 | Steps 3.3 to 3.9 complete. Phase 3 done. Added data-pipeline note under Current Status. Corrected vendor HQ count in src-01 notes (12 US, 11 India). |
| 2026-09-15 | Phase 4 reworked: added working rule (one step at a time, gated sources handled by the user), a candidate-list deliverable for 4.1, and a user checkpoint step 4.1b. |
| 2026-09-15 | Step 4.1 complete: 11 web candidates listed with access status in `content/web-candidates.md`. BCG PDF saved. 4.1b marked in progress, waiting on user. |
| 2026-09-15 | Step 4.1b complete: 10 new sources downloaded, registered and extracted. Added `extract_web.py`, page-range support in `extract_pdf.py`. Source inventory added under Current Status. |
| 2026-09-15 | Step 4.2 started. BCG (src-05) synthesised. 4.2 now carries a per-source progress list. |
| 2026-09-15 | Wrote `content/synth/README.md`, the step-by-step procedure for synthesising a source, so a fresh session can continue 4.2 without prior context. Linked from Current Status and CLAUDE.md. |
| 2026-09-15 | Step 4.2: Deloitte (src-04) synthesised. Next source is KPMG (src-09). |
| 2026-09-15 | Step 4.2: KPMG (src-09) synthesised. Next source is Menlo Ventures (src-07). |
| 2026-09-15 | Step 4.2: Menlo Ventures (src-07) synthesised. Added a Current Status note on reading web-source charts via the scratchpad. Next source is a16z (src-08). |
| 2026-09-15 | Step 4.2: a16z (src-08) synthesised. Next source is Microsoft Work Trend Index (src-10). |
| 2026-09-15 | User asked for the rest of Phase 4 in one run. Steps 4.2 (src-10, src-11, src-03, src-06 via forked agents; src-12 by the main agent), 4.3 and 4.4 completed. Phase 4 done. Current Status moved to Phase 5 with a reading guide for the data files. Decisions logged on VC vendor-share conflicts, leading-group range and web-source charts. Added `scripts/originality_check.py` and a first overlap report as a head start for 7.1 (not a Phase 4 step; recorded there, not acted on). |
| 2026-09-15 | User decisions recorded: Phase 6 paused; hosting is GitHub Pages from rbndchsn/genai_market. Added steps 5.0 (repo setup with exclusions) and 5.10 (Pages deploy), reworded 5.1 and 7.4, closed Q2, added Q6. Repo checked: exists, public, empty; local folder not yet a git repo. |
| 2026-09-15 | Step 5.0 done: repo initialised, exclusions in place, first commit pushed. Q6 resolved (synth notes stay local). Next step 5.1. |
| 2026-09-16 | Step 5.1 done: site shell (CSS tokens, dark mode, shell script, favicon, seven placeholder pages), reviewed with headless Chrome screenshots. Committed and pushed as 92e6eaf. Next step 5.2. |
| 2026-09-16 | Step 5.2 done: overview dashboard driven by the data files; chart colour tokens validated and added. Committed and pushed as 0fe5c2e. Next step 5.3. |
| 2026-09-16 | Step 5.3 done: insights page with filters, grouped expandable cards and deep links. Committed and pushed as 18b8f2b. Next step 5.4. |
| 2026-09-16 | Session handoff: added the "Phase 5 build and review routine" section under Current Status, `scripts/review_shots.py` (desktop and 390px screenshots in one command), Phase 5 rules in `CLAUDE.md`, and a development section in `README.md`. A fresh session reads `plan.md` and continues at 5.4. |
| 2026-09-16 | Step 5.4 done: vendors page (quadrant, table, drawer, deep links). `review_shots.py` gained `--theme` and `--iframe`; routine text updated. Q5 resolved. Decision logged to run 5.4 to 5.10 in one pass with a commit per step. Next step 5.5. |
| 2026-09-16 | Step 5.5 done: statistics page with 259 data-driven charts, table twins and tooltips. Next step 5.6. |
| 2026-09-16 | Step 5.6 done: glossary page. Next step 5.7. |
| 2026-09-16 | Step 5.7 done: sources page (bibliography, method, evidence levels, licensing, citation, data files). Next step 5.8. |
| 2026-09-16 | Step 5.8 done: search index builder, search page, header search. Data pipeline note now includes `build_search_index.py`. Next step 5.9. |
| 2026-09-16 | Step 5.9 done: link, anchor and console check (`scripts/check_site.py`) plus a full screenshot sweep. Next step 5.10. |
| 2026-09-16 | Step 5.10 done: Pages workflow, Pages enabled, live at https://rbndchsn.github.io/genai_market/. Phase 5 complete. Current Status moved to Phase 7 (next 7.1 originality review); hosting note and layout note updated. |
| 2026-09-16 | Step 7.0 added and done: warm colour palette across the site, chart tokens re-validated on the new surfaces. Next step remains 7.1. |
| 2026-09-16 | Headline changed to "A meta-analysis of the state of enterprise AI across twelve sources" in the home heading, footer, citation line and README; agentic AI named in the home lead. Decision logged. |
| 2026-09-16 | Headline revised to "A Meta-Analysis of the State of Enterprise AI" (title case, no source count) in the home heading, browser title, footer, citation line and README. |
| 2026-09-16 | Step 7.0b: theme, evidence and quadrant colours plus the hero band across the site. Next step remains 7.1. |
| 2026-09-16 | Step 7.1 done for the licensed source: 30 records rewritten, zero SRC-01 overlap; data validated, chunks and search index rebuilt, pushed. Next: optional 7.1b for public-source overlap, then 7.2. |
| 2026-09-16 | Removed the Data files section, its on-page nav link, the script that filled it and its CSS from `sources.html`, `sources.js`, `site.css`. Decision logged. `check_site.py` clean; Sources page reviewed at desktop and phone. Next step unchanged. |
| 2026-09-16 | Removed the How to cite section, its nav link and the citation-example script from `sources.html` and `sources.js`. Decision logged. `check_site.py` clean; page reviewed. Next step unchanged. |
| 2026-09-16 | Removed the Corrections paragraph from `sources.html` and the Repository link (and `REPO_URL`) from the footer in `shell.js`. Decision logged. `check_site.py` clean; footer reviewed. Next step unchanged. |
| 2026-09-16 | Search page: added a one-paragraph note under the lead saying the search is plain keyword matching in the browser, not an AI assistant, plus a collapsed "How this search works" block (what it does, what it does not do, semantic search vs RAG vs chat assistant, why keyword search was chosen). User request, so readers do not mistake it for RAG. Files: `site/search.html`, `site/css/site.css` (`.search-note`, `.search-about`). Reviewed collapsed (light) and open with results (dark) at desktop and phone; `check_site.py` clean. Not committed. |
| 2026-09-16 | Step 7.0c added and done: tag sphere replaces the ten starting-point pills on the Search page (72 curated tags in `search-tags.json`). Not committed. Next step unchanged. |
| 2026-09-16 | Step 7.0c second pass: true sphere projection with perspective and a tilted tumble, smooth hover pause, desktop 60 tags, phone 26. Pushed. |
| 2026-09-16 | Footer: the copyright line is replaced by "This online tool was created by:" with a link to the author's LinkedIn profile (`shell.js`). User request. |
| 2026-09-16 | Step 7.0d added and done: `method.html` (How it was built) with `method.js` (scale tiles from the data files), nav item, footer links (How it was built, Repository restored), cross-link from the Sources method section, `check_site.py` page list, README note. Three decision rows from earlier today reworded as plain statements of the decision (same decisions, no change in substance) so the public log reads as a record, not a defence. |
| 2026-09-16 | Step 7.2 done: accuracy evaluation of 20 sampled records, 8 corrections, results published on `method.html` from new `site/data/quality.json`. Terminology fixed for the site: validation (automated structure and link checks), originality check (scan plus review), accuracy evaluation (sampled, judged against cited pages). The method page's Quality phase text corrected to say the evaluation is published rather than implying it had already run. |
| 2026-09-16 | Method page: added a high-level "What the evaluation found" narrative (three paragraphs from `quality.json` `accuracy.findings`: eight problems in twenty, the two real errors, the pattern that chart-read numbers were all right and the prose around them was where slips occurred, all corrected before publishing) and folded the record-by-record table into a collapsed block. User request, so readers see that failures were found and fixed. |
| 2026-09-16 | Method page takeaways: one bullet added to each column on checking work against the source (beginners) and on separating validation from evaluation and budgeting for both (leads). User request. |
| 2026-09-16 | Step 7.2b added and done: full accuracy evaluation of 402 records (every insight, vendor profile and statistic; 20 glossary terms), 166 records corrected, results and per-record notes published on `method.html` from `quality.json`. Method page text updated (models used, phases, takeaways, fourth lesson). Evidence counts changed (47 multi, 32 single, 16 analysis). Repository issues, wiki and projects switched off earlier the same day. |
| 2026-09-16 | Method page: new section "Evaluating AI-written research" (why evaluation matters, why a sample was not enough, the ten-step procedure, run figures read from `quality.json` `accuracy.run`, lessons), linked from the page menu and the phases list. User request, to teach the importance of evaluation. |
| 2026-09-17 | Method page: added "Why the first draft was wrong so often" to the evaluation section (one in twelve records wrong, a third loose; causes: compression in multi-source synthesis, drift through repeated paraphrase, uncalibrated chart reading, self-checking against notes rather than pages, rules that invited convergence claims, volume without an accuracy step). User request after discussing why the first draft had so many errors. |
| 2026-09-17 | Plan tidy-up at the user's request: status block rewritten (date, phase, next step set to a discussion with the five options and the standing site rules), four decisions from 2026-09-16 moved from the change history to the decisions log, step 7.2c added for the unchecked glossary terms, step 7.4 reworded as the custom-domain step tied to Q4. No site changes. |
| 2026-09-17 | Session handoff: the accuracy-evaluation toolkit (three workflows and three helpers) saved to `scripts/eval/` with a procedure README; `.gitattributes` keeps those workflow scripts on LF; `scripts/eval/work/` git-ignored. `CLAUDE.md` gained the accuracy rules learned in 7.2b, the publishing rules, Windows pitfalls, and instructions to wait when a discussion is pending and to check which plan table a row belongs to. `README.md` status and quality sections updated. |
| 2026-09-17 | Step 7.5 done: README gained local-run requirements and an "Add a new source" procedure. Step 7.3 done: Lighthouse baseline on the live site, layout-shift and accessibility fixes (shell script moved to the top of `<body>` on all pages, scrollbar gutter, space reserved for data containers while empty, search filter role, tag size, method heading order), local re-measure at 96 to 100. 7.2c annotated as optional. Status block updated; changes not committed. |
| 2026-09-17 | 7.3 and 7.5 committed and pushed (4ac3f27). Step 7.6 added and done: licensing for reuse (MIT code, CC BY 4.0 text and data, AIM-derived material excluded), decision logged. Not committed. |
